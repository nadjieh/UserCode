module SingleTop = SingleTop {
	bool   isCSA07Soup       =  true
        bool   isFullSim         =  true
	untracked uint32 verbosity         =  9999

	# Cuts
	untracked double cutPtMinFirstMuon = 30.
	untracked double cutPtMinLeptons = 10.
	untracked double cutEtaFirstMuon = 2.1
	untracked double cutEtaLeptons = 2.1
	untracked double cutPtMinCentralJets = 30.
	untracked double cutPtMinForwardJets = 50.
	untracked string btagAlgo = 'combinedSVJetTags'
	untracked double cutBtag = 0.
	untracked double cutEtaJets = 2.5
	untracked double cutMET = 40.
	untracked double cutCaloIso = 5. # almost a "non-cut"
	untracked double cutTrackIso = 1.
	untracked double cutUpMTW = 100.
	untracked double cutLowMTW = 50.
}
